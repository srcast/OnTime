enum AnalysisMapEntriesEnum { minutesWorked, profit }

enum AppThemeMode { light, dark, system }

enum LanguageOptions { english, portuguese, french }

enum TutorialIdentifiers {
  checkIn,
  listItem,
  editPoint,
  deletePoint,
  configOption,
  hourValueInput,
  saveHourValue,
  specialRules,
}
