/* import 'package:flutter_test/flutter_test.dart';
import 'package:mockito/mockito.dart';
import 'package:on_time/database/database.dart';
import 'package:on_time/helpers/dates_helper.dart';
import 'package:on_time/viewmodel/home_page_vm.dart';

import '../mocks/mocks.mocks.dart'; */

void main() {
  /* test('getPointsSession deve retornar lista de pontos', () async {
    final mockService = MockPointsService();
    final viewModel = HomePageViewModel(mockService);

    final fakePontos = [
      Ponto(
        sessionId: DatesHelper.getSessionFromDate(DateTime.now()),

        date: DateTime.now(),
      ),
    ];

    when(mockService.getPointsSession(any)).thenAnswer((_) async => fakePontos);

    await viewModel.getSessionPoints();

    expect(1, 1);
  }); */
}
